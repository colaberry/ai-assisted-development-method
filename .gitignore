# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo

# Claude Code local settings (per-user, not for the repo)
.claude/settings.local.json

# Claude Code agent worktrees — scratch space for isolated agent runs.
# Never commit these.
.claude/worktrees/

# Editors
*.swp
.idea/
.vscode/

# Private content — NEVER committed.
# Use these folders for client notes, engagement-specific writeups, internal
# meta-discussion of how this methodology is being applied to a real client,
# or anything else you want kept off the public repo.
# Anything matching these patterns is ignored everywhere in the tree.
clients/
private/
*.private.md
*.private/

